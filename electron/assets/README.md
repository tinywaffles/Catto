Place the following files in this directory before packaging:

- icon.ico  — Windows application icon (256×256 recommended, .ico format)
- tray.png  — System tray icon (16×16 or 32×32 PNG, white/light on transparent background)

If these files are absent, Electron will use its default icon and an empty tray icon.
The app will still launch correctly without them.
